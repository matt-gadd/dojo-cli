module.exports = {
	name: 'my-build',
	description: 'this is my build command',
	options: [
		'name'
	],
	register(yargs) {
		return yargs.option('url', {
			alias: 'u',
			default: 'http://yargs.js.org/'
		})
	},
	run(argv) {
		console.log(`ran ${argv}`);
	}
}
